# 更新日志 / Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 正式版

### 功能
- **批量翻译**：txt / json / md / csv / srt / log / text，支持文件夹递归、镜像输出目录，不覆盖原文件。
- **实时进度条**：按 AI 请求批次推进，翻译大文件也能看到进度；日志显示每个文件的请求次数与耗时。
- **多语言互译**：主界面「源语言 → 目标语言」双下拉，任意组合（英→韩、日→越、中→日…），另有「自动识别」按文字体系判断语种。
- **多接口预设**：设置里可保存多组 Base URL + API Key + 模型，下拉一键切换；旧配置自动迁移为「默认」预设。
- **对话翻译面板**：左右双面板（类 Google 翻译），大字号输入/译文、示例短语、风格（默认/非正式/正式）、复制、朗读；**点击「▶ 翻译」才请求**，不随输入自动消耗额度。
- **朗读可控**：再次点击停止朗读；关闭面板或退出程序会立即终止后台朗读进程。
- **格式细节**：JSON 保留结构与键名（可选翻键名）、自动跳过 URL/路径/数字/UUID/代码标识符；SRT 保留序号与时间轴；CSV 保留表头与分隔符；输出编码可选 UTF-8 / UTF-8 BOM / GBK。
- **零依赖**：纯 Python 标准库（tkinter + urllib），无需 pip 安装。

### 工程
- 统一项目标识为 **AI Batch Translator / AI 批量翻译工具**，窗口标题与日志显示版本号。
- 新增 `.gitignore`（重点保护含 API Key 的 `config.json`）、`.gitattributes`（统一 LF 换行、二进制标记、语言统计排除示例）、`requirements.txt`、`CHANGELOG.md`、Issue 模板。
- 新增 `README.md`（中文）与 `README.en.md`（英文），含徽章、截图、格式支持表与常见问题。
- 示例文件目录 `示例/` 重命名为 `samples/`；新增 ASCII 启动脚本 `start.bat`。

### 移除
- 早期版本内置的本地「英汉词典」离线翻译模式及其 `Dictionary` 文件夹、词典模块（历史版本仍可在提交记录中查看）。

## [0.1.0] - 初版

- 基于本地英汉词典的离线逐词翻译。
- 支持 txt / json / csv / srt 等格式的批量处理与文件夹递归。
